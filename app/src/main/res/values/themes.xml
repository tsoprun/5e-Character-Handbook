<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.DnD5eCompanion" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

        <!-- akcnet -->
        <item name="colorPrimary">@color/gold</item>
        <item name="colorPrimaryVariant">@color/bronze_dim</item>
        <item name="colorOnPrimary">@color/black</item>

        <item name="colorSecondary">@color/blood</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- pozadine i povrsine -->
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/gold_light</item>
        <item name="colorOnBackground">@color/gold_light</item>

        <!-- tekst -->
        <item name="android:textColorPrimary">@color/gold_light</item>
        <item name="android:textColorSecondary">@color/text_muted</item>

        <item name="android:statusBarColor">?attr/colorSurface</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.DnD5eCompanion">
        <item name="android:textSize">@dimen/_50sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">cursive</item>
    </style>
</resources>